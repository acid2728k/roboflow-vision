{
  "name": "roboflow-vision",
  "version": "0.1.0",
  "description": "Web app for real-time object detection and scene caption from the camera (vanilla JS, Vite, optional Express proxy for Roboflow).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node backend/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
